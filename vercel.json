{
  "version": 1,
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],
  "builds": [{ "src": "*.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
